{
  "name": "web-research-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "An MCP server that gives any AI assistant safe, structured web access — fetch, extract, and summarize pages.",
  "type": "module",
  "bin": {
    "web-research-mcp": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@google-cloud/vertexai": "^1.12.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "cheerio": "^1.0.0",
    "robots-parser": "^3.0.1",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "^1.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
